{
	// Place your oi 工作区 snippets here. Each snippet is defined under a snippet name and has a scope, prefix, body and 
	// description. Add comma separated ids of the languages where the snippet is applicable in the scope field. If scope 
	// is left empty or omitted, the snippet gets applied to all languages. The prefix is what is 
	// used to trigger the snippet and the body will be expanded and inserted. Possible variables are: 
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. 
	// Placeholders with the same ids are connected.
	// Example:
	// "Print to console": {
	// 	"scope": "javascript,typescript",
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
	"common": {
		"scope": "cpp",
		"prefix": "#include",
		"body": [
			"#include <bits/stdc++.h>",
			"",
			"signed main() {",
			"#ifndef ONLINE_JUDGE",
			"    freopen(\"${TM_FILENAME_BASE}.in\", \"r\", stdin);",
			"#endif  // ONLINE_JUDGE",
			"    std::cin.tie(nullptr)->sync_with_stdio(false);",
			"",
			"    $1",
			"}",
			""
		]
	},
	"fast-io": {
		"prefix": "#include",
		"body": [
			"#include <bits/stdc++.h>",
			"#include <sys/mman.h>",
			"#include <sys/stat.h>",
			"",
			"namespace IO {",
			"",
			"char* rp = (char*)[]() {",
			"#ifndef ONLINE_JUDGE",
			"    freopen(\"${TM_FILENAME_BASE}.in\", \"r\", stdin);",
			"#endif  // ONLINE_JUDGE",
			"    struct stat st;",
			"    fstat(STDIN_FILENO, &st);",
			"    return mmap(NULL, st.st_size, PROT_READ, MAP_PRIVATE, STDIN_FILENO, 0);",
			"}",
			"();",
			"",
			"inline char get() { return *rp++; }",
			"",
			"// #define get() getchar_unlocked()",
			"",
			"template <typename T>",
			"inline void fast_read(T& x) {",
			"    char c;",
			"    x = 0;",
			"    T neg = 1;",
			"    while (!isdigit(c = get())) {",
			"        if (c == '-') neg = -1;",
			"    }",
			"    do x = x * 10 + neg * (c - '0');",
			"    while (isdigit(c = get()));",
			"}",
			"",
			"template <typename T>",
			"inline void fast_read_u(T& x) {",
			"    char c;",
			"    x = 0;",
			"    while (!isdigit(c = get()));",
			"    do x = x * 10 + (c - '0');",
			"    while (isdigit(c = get()));",
			"}",
			"",
			"template <typename T, typename... Args>",
			"inline void fast_read(T& first, Args&... args) {",
			"    fast_read(first), fast_read(args...);",
			"}",
			"",
			"template <typename T, typename... Args>",
			"inline void fast_read_u(T& fst, Args&... args) {",
			"    fast_read_u(fst), fast_read_u(args...);",
			"}",
			"",
			"#ifdef FAST_WRITE",
			"",
			"#define BUFSIZE (1 << 20)",
			"char wbuf[BUFSIZE], *wp = wbuf;",
			"",
			"inline void flush() {",
			"    fwrite(wbuf, 1, wp - wbuf, stdout);",
			"    wp = wbuf;",
			"}",
			"",
			"inline void put(const char& c) {",
			"    if (wp - wbuf == BUFSIZE) flush();",
			"    *wp++ = c;",
			"}",
			"",
			"// #define put(x) putchar_unlocked(x)",
			"",
			"template <typename T>",
			"inline void fast_write(T x) {",
			"    T neg = 1;",
			"    if (x < 0) {",
			"        neg = -1;",
			"        put('-');",
			"    }",
			"    static char st[40];",
			"    char* tp = st;",
			"    do {",
			"        *tp++ = '0' + neg * (x % 10);",
			"        x /= 10;",
			"    } while (x);",
			"    while (tp != st) put(*--tp);",
			"}",
			"",
			"template <typename T>",
			"inline void fast_write_u(T x) {",
			"    static char st[40];",
			"    char* tp = st;",
			"    do {",
			"        *tp++ = x % 10 + '0';",
			"        x /= 10;",
			"    } while (x);",
			"    while (tp != st) put(*--tp);",
			"}",
			"",
			"#endif  // FAST_WRITE",
			"}  // namespace IO",
			"",
			"signed main() {",
			"#ifndef FAST_WRITE",
			"    std::ios::sync_with_stdio(false);",
			"#endif  // FAST_WRITE",
			"",
			"    $1",
			"",
			"#ifdef FAST_WRITE",
			"    IO::flush();",
			"#endif  // FAST_WRITE",
			"}",
			""
		]
	}
}