// {P3386}
#include <bits/stdc++.h>

int n, m, e;
std::vector<std::vector<int>> g;

signed main() {
#ifndef ONLINE_JUDGE
    freopen("maximum-matching-bipartile.in", "r", stdin);
#endif  // ONLINE_JUDGE
    std::cin.tie(nullptr)->sync_with_stdio(false);
    std::cin >> n >> m >> e;
}
