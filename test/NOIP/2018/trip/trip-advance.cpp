#include <bits/stdc++.h>

using std::cin, std::cout, std::endl;
using std::vector, std::pair, std::tuple;

constexpr char lf = '\n';
constexpr char sp = ' ';

signed main() {
#ifndef ONLINE_JUDGE
    freopen("trip-advance.in", "r", stdin);
#endif
    std::cin.tie(nullptr)->sync_with_stdio(false);
    // TODO
}
